--- cPanel MCP Server Configuration ---
This is an example file. Copy this to .env and fill in your actual credentials.
The .env file is ignored by git to keep your secrets safe.
The full URL to your WHM server, including the secure port (2087).
CPANEL_HOST="https://www.google.com/search?q=https://your-whm-host.com:2087"

The WHM username that has API privileges (e.g., 'root' or a reseller).
CPANEL_USER="your_whm_root_or_reseller_user"

The WHM API Token generated from WHM > Development > Manage API Tokens.
CPANEL_API_TOKEN="your_whm_api_token_goes_here"